# References — {Client Name}

This folder holds **pointers**, not copies. The WEF framework is the source of truth; do not
duplicate its content here (Governance Sec. 5.2.1 Rule 3).

- **Framework:** `{relative path to the WEF repo}/WEF-v1.0/` — Core Methodology (Stage Gate
  spine), Component Library, Front/Back Matter.
- **Active Industry Module(s):** `{...}/WEF-v1.0/Industry-Modules/{Module-File}.md` —
  {module name(s)}, selected per `DEC-INIT-001` in `../_config/Decision-Register.md`.
- **Blend boundary (if applicable):** {which pages/sections follow the secondary module}.

When a Stage Gate needs framework content, read it from these paths — don't paste it into the KB.
